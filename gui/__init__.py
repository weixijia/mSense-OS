# GUI module — PySide6 + Qt Quick (QML) presentation layer.
# The live-frame bridge and app controller live in gui.qml_bridge;
# the QML UI lives in gui/qml/. Kept import-light so importing this
# package never pulls in a second Qt binding.
