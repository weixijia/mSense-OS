# Vomee Multi-Modal Data Capture System Dependencies

# Core
numpy>=1.21.0
opencv-python>=4.5.0
Pillow

# GUI (PySide6 + Qt Quick / QML)
PySide6>=6.5.0

# Pose estimation — ViTPose engine (default) + YOLOv8 detection/tracking
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
filterpy            # SORT tracker
scipy
scikit-image
matplotlib
ffmpeg-python       # used by the vendored ViTPose visualization utils

# Pose estimation — MediaPipe (fallback backend)
mediapipe>=0.9.0

# Optional: GPU-accelerated mmWave FFT processing (NVIDIA only).
# The mmWave processor automatically falls back to NumPy if CuPy is absent.
# Install the build matching your CUDA version, e.g.:
#   pip install cupy-cuda11x   # CUDA 11.x
#   pip install cupy-cuda12x   # CUDA 12.x

# Notes:
# - ViTPose runs on CUDA > MPS (Apple) > CPU automatically.
# - The smallest model (vitpose-s) ships in ./models or auto-downloads on first run.
